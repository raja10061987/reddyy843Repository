({
	add : function(component, event, helper) {
      // let firstNumber = parseInt(component.find('fNumber').get('v.value'));
      // let secondNumber = parseInt(component.find('sNumber').get('v.value'));
      // component.find('result').set('v.value', firstNumber + secondNumber);
        helper.addHelper(component,event,helper);
        
        
 	}
})